import ContentManager from './ContentManager';

export default function Duas() {
  return (
    <ContentManager
      tableName="duas"
      title="Duas"
      subtitle="Manage daily duas mapped to the Hijri calendar"
      contentLabel="Dua"
    />
  );
}
